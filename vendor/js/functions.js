/**
 * descripción: Funciones auxiliares de los módulos.
 */

 /**
  * Genera pares ordenados correspondientes a los lados del grafo dirigido y su
  * correspondiente magnitud.
  * @param {*} vertexNumber : int Número de vertices que tiene le grafo. 
  * @returns Array contiene tripletas {origen, destino, longitud}
  */
 function generateRandomSides(vertexNumber) {
     return new Array();
 }